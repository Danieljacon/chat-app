import React from 'react'
import { Container } from './styles.js'

const BarTop = ({ children }) => {
    return (
        <Container>
            {children}
        </Container>
    )
}

export default BarTop
